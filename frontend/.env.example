# Optional. Without it, AI features fall back to on-device heuristics.
VITE_HF_API_KEY=
# Optional. Create an OAuth 2.0 Web client in Google Cloud Console, add your
# origin (http://localhost:5173) to "Authorised JavaScript origins".
VITE_GOOGLE_CLIENT_ID=
